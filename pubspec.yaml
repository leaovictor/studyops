name: studyops
description: "StudyOps — Intelligent study performance management SaaS."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  google_sign_in: ^6.2.1

  # State management
  flutter_riverpod: ^2.5.1

  # Navigation
  go_router: ^14.2.7

  # Charts
  fl_chart: ^0.69.0

  # UI / Fonts
  google_fonts: ^6.2.1
  table_calendar: ^3.1.2
  lottie: ^3.1.2
  animations: ^2.1.1

  # Flashcards
  fsrs: ^2.0.1
  flash_card: ^0.1.0

  # Utilities
  intl: ^0.19.0
  uuid: ^4.4.2
  dart_openai: ^5.1.0
  file_picker: ^8.1.7
  crypto: ^3.0.6
  cupertino_icons: ^1.0.8
  confetti: ^0.8.0
  flutter_staggered_animations: ^1.1.1
  audioplayers: ^6.5.1
  wakelock_plus: ^1.2.8
  el_tooltip: ^2.2.1
  pull_to_refresh: ^2.0.0
  http: ^1.6.0

  shared_preferences: any
  flutter_slidable: ^4.0.3
  cloud_functions: ^5.6.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/song/
